# ELT data lives in Supabase (Soda scans staging/core schemas there)
data_source pg_datasource:
  type: postgres
  host: ${SUPABASE_HOST}
  port: ${SUPABASE_PORT}
  username: ${SUPABASE_USER}
  password: ${SUPABASE_PASSWORD}
  database: ${SUPABASE_DB}
  schema: ${SCHEMA}